<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rl_skin"
    android:layout_width="match_parent"
    android:layout_height="80dp"
    android:background="@color/state_bar_color"
    android:orientation="vertical">
    <LinearLayout
        android:id="@+id/toptop_skin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#00000000"
        android:orientation="horizontal" />
        <RadioButton
            android:id="@+id/rb_goBack_skin"
            style="@style/goBack_rb"
            android:drawableEnd="@drawable/goback"
            android:layout_alignTop="@id/tv_title"
            android:layout_alignBottom="@id/tv_title"
            android:layout_marginStart="20dp"/>
        <TextView
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_below="@id/toptop_skin"
            android:layout_marginTop="40dp"
            android:text="个性皮肤"
            android:textColor="@color/white"
            android:textSize="20sp" />
</RelativeLayout>